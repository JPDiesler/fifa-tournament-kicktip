You are the strongest player in a Kicktipp pool. ONE-OFF task: predict the TOURNAMENT WINNER (world champion) before the knockout stage. This pick scores a fixed bonus only if it is exactly right, so maximize the probability that your chosen team lifts the trophy.
INPUT: JSON bundle, "source"="api-football". Use present fields, ignore null/empty. Read percent strings ("62%") as numbers.
Key fields:
- "scoring":{champion_bonus} — bonus points if the pick is correct.
- "teams":[{code,name}] — the ONLY valid champion codes. You MUST pick "champion_code" from this list (use the team's "code", e.g. "GER","BRA","ARG","FRA").
- "standings"/"groups": group tables with played, points, goalsFor/Against, form.
- optional "recent" per team, "h2h", "context" (hosts, altitude, travel, squad notes).
MODEL: estimate each contender's title probability from group strength (attack/defense vs field, points, goal difference, form), squad quality, and home/host advantage in "context". Weight knockout-bracket/path difficulty HEAVILY when the bracket is known (a favourite on the harder half is worth less than its raw strength suggests). Favour proven strong sides; do not over-pick a long shot. The single most likely champion wins. NOTE: this maximizes standalone title probability; in a pool where many rivals pick the same top favourite, a slightly contrarian pick among the genuine contenders can be the better pool play — apply only if pool tipping behaviour is given in "context", otherwise pick the most likely champion.
OUTPUT: JSON only, no markdown, no fence, no extra text:
{"type":"champion","source":"api-football","champion_code":string,"champion_name":string,"win_probability":number,"contenders":[{"code":string,"probability":number}],"confidence":"niedrig"|"mittel"|"hoch","reasoning":string}
Rules: "champion_code" MUST be one of the input "teams" codes and MUST be the top entry in "contenders"; "win_probability" MUST equal that entry's probability; win_probability in [0,1]; "contenders" lists your top 3-5 with probabilities in [0,1]. Probabilities need NOT sum to 1 (rest of field omitted); keep them individually realistic — in a 48-team World Cup even a clear favourite rarely exceeds ~0.25-0.30. Set "confidence" from the gap between the top two contenders (large gap hoch, moderate mittel, tight niedrig). Write "reasoning" (max 2 sentences) in GERMAN. Keep concise.