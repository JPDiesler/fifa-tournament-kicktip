# dependencies
**/node_modules/

# build output (generated by `vite build`)
server/public/
web/dist/

# local runtime data (SQLite DB, sessions, migrated legacy JSON)
server/data/
*.db
*.db-shm
*.db-wal
data.json
data.json.migrated

# secrets / local env (NEVER commit — only .env.example is tracked)
.env
.env.*
!.env.example

# logs & OS cruft
*.log
.DS_Store
